<resources>
  <!-- Default screen margins, per the Android Design guidelines. -->
  <dimen name="activity_horizontal_margin">16dp</dimen>
  <dimen name="activity_vertical_margin">16dp</dimen>
  <dimen name="appbar_padding">16dp</dimen>
  <dimen name="appbar_padding_top">8dp</dimen>

  <!-- Attraction Photo Height -->
  <dimen name="attractionPhotoHeight">100dp</dimen>

  <!-- Attraction Photo Width -->
  <dimen name="attractionPhotoWidth">150dp</dimen>

  <!-- Tab Layout Text Size -->
  <dimen name="customTabTextSize">10sp</dimen>

  <!-- Default List Views Margin -->
  <dimen name="defaultListViewMargin">5dp</dimen>

  <!-- Margin Below Attraction Name -->
  <dimen name="infoMarginBelowName">10dp</dimen>

  <!-- Margin Below Attraction Name -->
  <dimen name="photoMarginBelowName">20dp</dimen>

  <!-- List Item Text Start Margin -->
  <dimen name="listItemTextStartMargin">5dp</dimen>

  <!-- Attraction Information Margin -->
  <dimen name="attractionInfoMargin">15dp</dimen>

  <!-- Attraction Address Text -->
  <dimen name="addressTextSize">12sp</dimen>

  <!-- Attraction Days/Hours Availability Text Size -->
  <dimen name="hoursTextSize">13sp</dimen>

  <!-- Attraction Name Text Size -->
  <dimen name="nameTextSize">20sp</dimen>

  <!-- Alert Title Text Size -->
  <dimen name="alertTitleTextSize">16dp</dimen>

  <!-- Card View Layout Margin -->
  <dimen name="marginCardView">10dp</dimen>

  <!-- Card View Elevation -->
  <dimen name="elevationCardView">7dp</dimen>

  <!--Card View Corner Radius -->
  <dimen name="cornerRadiusCardView">7dp</dimen>
</resources>